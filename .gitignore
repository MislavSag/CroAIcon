_site/
.quarto/
.freeze/
preview/

.Rproj.user/
.Rhistory
.RData
.Ruserdata
renv/library/
renv/staging/

.venv/
__pycache__/
*.py[cod]

.env
.Renviron

data/raw/**
data/processed/**
data/warehouse/**
!data/**/.gitkeep
*.duckdb
*.duckdb.wal
*.parquet

# Non-refetchable / public reference series (e.g. transcribed Tica appendix)
# and the source registry are intentionally TRACKED. data/reference/ is not
# matched by the ignores above, but keep this explicit so it stays committed.
!data/reference/
!data/reference/**

drafts/ai/**
!drafts/ai/.gitkeep

outputs/figures/**
outputs/tables/**
outputs/facts/*.json
!outputs/**/.gitkeep
!outputs/facts/example_facts.json

.DS_Store
Thumbs.db
.vscode/


/.quarto/

**/*.quarto_ipynb

# Local-only research scratch (full DB schema incl. non-GFI tables)
_local/

# Editorial guidelines and writing procedures (local only)
editorial/

# Workflow layer, keep local only files out
.claude/settings.local.json
.claude/state/personal-memory.md
AGENTS.override.md
